{
  "name": "games-nooutco-me",
  "version": "1.0.0",
  "description": "ABA therapy games platform",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --port 8788",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
